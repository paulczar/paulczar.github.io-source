Source of Paul's Blog
=====================

preview:

```
$ hugo server --theme=hugo-uno --buildDrafts --watch --buildFuture
```


push to production:

```
$ git push origin master
$ hugo --theme=hugo-uno
$ ./deploy.sh
```
